# 1. انتقل إلى مجلد TIF-AI
cd "C:\Users\pc\OneDrive\Desktop\TIF\TIF-AI"

# 2. أنشئ وشغل بيئة افتراضية (اختياري لكن موصى به)
python -m venv venv
.\venv\Scripts\activate   # على Windows
# source venv/bin/activate  # على Linux/macOS

# 3. ثبّت الحاجات
pip install -r requirements.txt

# 4. نسخ مثال المتغيرات البيئية وتعديلها إذا لزم الأمر
copy .env.example .env
# (عدّل القيم في .env حسب احتياجاتك، مثلاً أضف مفتاح API réel)

# 5. شغّل الخادم
uvicorn app.main:app --reload



cd frontend
npm install
npm start